name: crescent
description: App para compra/venta de acciones con análisis predictivo
version: 1.0.0+1
environment:
  sdk: ">=2.17.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
flutter:
  uses-material-design: true
  assets:
    - assets/images/
